# Ruleset parser plugin #

## Description ##
This plugin is responsible to parse rulesets (including the selectors) and the ruleset declarations.

## Configuration: ##
  * `true` / `false`

## Defaults: ##
  * `true`